---
marp: true
theme: default
paginate: true
size: 16:9
lang: ko
header: 'SpeechNote — AI 음성 인식 메모장'
footer: '2026 프로젝트 발표 · 정의영 · 민건영'
style: |
  section {
    font-family: -apple-system, "Segoe UI", "Malgun Gothic", Roboto, sans-serif;
    font-size: 25px;
    background: #f8fafc;
    color: #1f2937;
    padding: 52px 60px;
  }
  h1 { color: #1d4ed8; font-size: 1.8em; }
  h2 { color: #1e3a8a; border-bottom: 3px solid #93c5fd; padding-bottom: 8px; }
  h3 { color: #2563eb; margin-bottom: 4px; }
  strong { color: #0f172a; }
  table { font-size: 0.68em; border-collapse: collapse; width: 100%; }
  th { background: #1e3a8a; color: #fff; }
  td, th { border: 1px solid #cbd5e1; padding: 5px 9px; }
  tr:nth-child(even) { background: #eef2ff; }
  code { background: #e5e7eb; padding: 1px 5px; border-radius: 4px; }
  pre { font-size: 0.6em; }
  .cols { display: grid; grid-template-columns: 1fr 1fr; gap: 26px; }
  .cols3 { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 18px; }
  .small { font-size: 0.82em; }
  .tag { display:inline-block; background:#dbeafe; color:#1e40af; border-radius:6px; padding:2px 10px; font-size:0.8em; margin:2px; }
  .box { background:#fff; border:1px solid #e2e8f0; border-radius:12px; padding:14px 18px; box-shadow:0 1px 2px rgba(0,0,0,.04); }
  .why { background:#ecfdf5; border-left:5px solid #10b981; padding:10px 16px; border-radius:8px; }
  .big { font-size:2.4em; font-weight:800; color:#1d4ed8; line-height:1.1; }
  .flow { font-size: 0.8em; line-height: 1.8; }
  section.lead { background: linear-gradient(135deg,#1e3a8a,#2563eb); color:#fff; justify-content:center; text-align:center; }
  section.lead h1 { color:#fff; font-size:2.5em; }
  section.lead h2 { color:#bfdbfe; border:none; }
  section.lead p { color:#dbeafe; }
  footer, header { color:#94a3b8; }
---

<!-- _class: lead -->
<!-- _paginate: false -->

# 🎙 SpeechNote

## 마이크와 시스템 음성을 함께 기록하는 AI 음성 메모장

**정의영**(팀장) · **민건영**  |  2026 프로젝트 발표

<!--
[오프닝 30초]
"안녕하세요. 저희 팀은 회의·강의 음성을 자동으로 텍스트와 요약으로 남겨주는 데스크톱 앱 'SpeechNote'를 만들었습니다. 발표는 왜 만들었는지부터, 어떻게 설계·구현했고, 실제로 동작하는지, 무엇을 배웠는지 순서로 진행하겠습니다."
- 너무 길게 끌지 말고 바로 다음 장(발표 순서)으로.
-->

---

## 발표 순서

<div class="cols">
<div>

**1. 왜 — 문제와 목표**
- 문제 정의 · 동기
- 목표와 범위

**2. 무엇을 — 요구사항·설계**
- 요구사항 분석
- 기술 선택 근거
- 아키텍처 · DB 설계

</div>
<div>

**3. 어떻게 — 구현**
- 프로젝트 구조 · 추적성
- 핵심 구현 · 기술적 난제

**4. 증명 — 동작·협업**
- 데모 · 테스트 · 배포 · 협업

**5. 마무리**
- 구현 현황 · 회고 · 결론

</div>
</div>

<!--
[10초] "발표는 크게 다섯 단계로 진행합니다." 빠르게 넘어가기. 청중에게 전체 지도를 주는 슬라이드.
-->

---

<!-- _class: lead -->

# 1. 왜 만들었나

## 문제 · 동기 · 목표

---

## 문제 정의 & 동기

<div class="cols">
<div>

### 🔴 현실의 불편함
- 화상회의·온라인 강의 내용을 **사람이 일일이 받아 적어야** 함
- 받아 적다 보면 **내용에 집중 못 함**
- 끝나면 누가 무슨 말을 했는지 흐릿함

### ⚠️ 기존 도구의 한계
- 특정 회의 플랫폼에 **종속**
- 보통 **내 마이크 목소리만** 기록 → 상대방(스피커) 음성은 놓침

</div>
<div>

### 🟢 우리의 접근
- **마이크 + 시스템(스피커) 음성을 동시에** 캡처
- 변환 결과를 **내 PC에 저장**하고 자유롭게 가공
- **AI 요약·키워드**로 핵심만 빠르게 파악

<div class="why">
<b>한 줄 정리</b><br>
"플랫폼에 묶이지 않고, 양쪽 목소리를 모두 기록해, 요약까지 해주는 내 노트"
</div>

</div>
</div>

<!--
[60초] 발표의 '후크'. 청중이 공감하도록 문제부터.
"여러분도 회의나 강의 들으면서 받아적느라 정작 내용을 놓친 경험 있으실 겁니다."
핵심 차별점 1가지를 못박기: '대부분 도구는 내 마이크만 녹음하지만, 저희는 상대방이 말하는 시스템 음성까지 함께 캡처합니다.'
[예상 질문] "클로바노트 같은 게 이미 있지 않나?" →
답: "맞습니다. 다만 그런 서비스는 클라우드 종속·플랫폼 제약이 있고, 저희는 순수 자바로 오디오 캡처·외부 API·DB·GUI를 직접 통합 구현하는 것이 학습 목표였습니다. 특히 시스템 음성 동시 캡처와 로컬 보관이 차별점입니다."
-->

---

## 목표와 범위 (Scope)

<div class="cols">
<div>

### 🟢 In-Scope (이번에 한 것)
- 마이크·시스템 **동시 녹음**
- 오디오 파일 업로드 변환
- 기록 **DB 저장·검색·정렬**
- TXT / DOCX / SRT **내보내기**
- 회원가입·로그인 (사용자별 분리)
- **AI 요약·키워드**

</div>
<div>

### 🔴 Out-of-Scope (의도적으로 제외)
- 실시간 스트리밍 변환
- 다중 기기 동기화(중앙 DB 서버)
- 고급 보안(salt · 2FA)
- 모바일 앱

<div class="why">
범위를 <b>명확히 그은 이유</b>: 3주 일정 안에서 "동작하는 완결된 데스크톱 앱"을 우선. 제외 항목은 회고에서 개선 방향으로 다룸.
</div>

</div>
</div>

<!--
[40초] 범위를 명확히 보여주는 것 자체가 '딴지 방어'.
교수님이 "이건 왜 안 했냐"고 물으면, 이미 Out-of-Scope에 적어둔 항목이고 "일정·인프라 제약으로 의식적으로 제외했으며 개선 방향으로 회고에 담았다"고 답하면 됨.
[예상 질문] "실시간 변환은 왜 못 했나?" → "현재는 녹음 종료 후 일괄 전송 방식입니다. 실시간은 WebSocket 스트리밍 STT가 필요해 범위를 넘어, 회고에서 개선안으로 제시했습니다."
-->

---

<!-- _class: lead -->

# 2. 무엇을

## 요구사항 분석 · 설계

---

## 요구사항 분석 (기능 20개, F-01 ~ F-20)

<div class="cols">
<div class="small">

| ID | 기능 | 담당 | 우선 |
| --- | --- | --- | --- |
| F-01 | 실시간 녹음·장치 선택 | 민 | 높음 |
| F-02 | 오디오 파일 업로드 | 민 | 높음 |
| F-03 | 커스텀 STT API | 민 | 높음 |
| F-05 | 비동기·프로그레스 | 민 | 높음 |
| F-06 | 회원가입·로그인 | 정 | 높음 |
| F-07 | SQLite DB 연동 | 정 | 높음 |
| F-13 | 핵심 요약 | 민 | 중간 |
| F-14 | 키워드 추출 | 민 | 중간 |
| F-19 | 전역 에러 로깅 | 정 | 높음 |
| F-20 | Two-Split UI | 정 | 높음 |

</div>
<div class="small">

| ID | 기능 | 담당 | 우선 |
| --- | --- | --- | --- |
| F-08 | 히스토리 패널 | 정 | 중간 |
| F-09 | 우클릭 메뉴 | 정 | 중간 |
| F-10 | TXT 내보내기 | 정 | 중간 |
| F-11 | DOCX·SRT 내보내기 | 정 | 중간 |
| F-12 | STT 원문 편집 | 정 | 중간 |
| F-15 | 실시간 통합 검색 | 정 | 중간 |
| F-16 | 목록 정렬 | 정 | 낮음 |
| F-17 | 계정 관리(변경·탈퇴) | 정 | 낮음 |
| F-18 | 오디오 미니 플레이어 | 정 | 낮음 |
| F-04 | 설정·구성 관리 | 민 | 낮음 |

</div>
</div>

<!--
[50초] 모든 기능을 읽지 말 것. "요구사항을 20개로 정의하고, 담당자와 우선순위를 정해 GitHub Issues로 추적했습니다. 높은 우선순위(녹음·STT·DB·UI)를 1·2주차에 먼저 완성하고, 부가 기능을 3주차에 붙였습니다." 정도로.
[예상 질문] "요구사항은 어떻게 도출했나?" → "문제 정의에서 출발해 입력(녹음/업로드)·처리(STT/AI)·관리(DB/검색/내보내기)·계정 4영역으로 나눠 기능을 도출하고, 비기능 요구사항(성능·보안·안정성)을 별도로 정의했습니다."
[예상 질문] "우선순위 기준은?" → "핵심 가치(음성→텍스트→저장)에 직접 닿는 기능을 '높음'으로 두었습니다."
-->

---

## 비기능 요구사항 & 기술 선택 근거

<div class="cols">
<div>

### 비기능 요구사항
- **성능**: 1분 음성 → 30초 내 변환
- **사용성**: 3-클릭 내 변환
- **안정성**: 오류 시 종료 없이 안내
- **보안**: 키 분리, 비번 해시
- **배포성**: 단일 JAR, 설치 불필요

</div>
<div class="small">

### 왜 이 기술인가
| 기술 | 선택 이유 |
| --- | --- |
| **Java 11 / Swing** | 추가 런타임 없이 크로스플랫폼 GUI, Java Sound로 오디오 캡처 가능 |
| **SQLite** | 서버 불필요, 파일 1개로 동작 → 로컬 앱에 최적 |
| **외부 STT/LLM API** | 인식·요약 모델을 직접 만들 수 없으니 검증된 API에 집중 |
| **Maven + shade** | 의존성 관리 + 단일 실행 JAR 패키징 |
| **FlatLaf / POI** | 현대적 UI / DOCX 문서 생성 |

</div>
</div>

<!--
[50초] 이 슬라이드가 기술 관련 딴지를 막는 핵심.
각 선택에 '왜'가 붙어 있다는 점을 강조: "기술은 익숙해서가 아니라 목적에 맞춰 골랐습니다."
[예상 질문] "왜 SQLite냐, 실무는 MySQL 쓰는데?" → "단일 사용자 로컬 앱이라 서버·설치가 필요 없는 SQLite가 적합했습니다. 다중 기기 동기화가 필요하면 중앙 DB로 가야 하고, 그건 회고의 개선 방향입니다."
[예상 질문] "STT/LLM을 직접 안 만들고 API 쓴 건 너무 쉬운 거 아니냐?" → "음성인식·LLM 모델 학습은 과목 범위 밖이고, 저희 학습 목표는 오디오 캡처·API 연동·비동기·DB·GUI 통합이었습니다. API 통신 모듈(재시도·타임아웃·깨진 JSON 복구)을 직접 견고하게 구현했습니다."
-->

---

## 시스템 아키텍처 (계층 분리)

<div class="flow">

```
[사용자] 마이크+시스템 / 파일 업로드
   │
   ▼
[ View · Swing UI ] ──▶ [ 입력 검증 · 흐름 제어 ]
   │                          │
   │                          ▼
   │                   [ Service · 음성 처리/API 연동 ] ──▶ [외부 AI API]
   │                          │                              STT · LLM 요약
   │                          ▼                                  │
   └──◀── [ 결과 처리: 텍스트/요약/키워드 ] ◀──────────────────────┘
                  │                  │
                  ▼                  ▼
          [ 결과 화면 ]        [ DAO · JDBC ] ──▶ [Local SQLite]
       복사/다운로드/관리                      users · transcripts · segments
```

</div>

**핵심 원칙**: UI ↔ Service ↔ API/DAO를 **단방향 의존**으로 분리 → 한 곳을 고쳐도 다른 곳에 영향 최소화

<!--
[50초] "왜 이렇게 나눴나"를 설명.
"화면 코드와 DB 코드가 섞이면 유지보수가 어렵습니다. View는 보여주기만, Service는 흐름 제어, API는 외부 통신, DAO는 저장만 담당하도록 역할을 나눴고, 의존 방향을 한쪽으로 고정해 결합도를 낮췄습니다. 두 팀원의 작업 경계도 이 계층을 따라 나뉘어 충돌이 적었습니다."
[예상 질문] "MVC 패턴이냐?" → "엄밀한 MVC라기보다 View / Service / Data 계층 분리에 가깝습니다. 공통 도메인 객체 TranscriptResult가 계층 사이 데이터 전달 역할을 합니다."
-->

---

## 데이터베이스 설계 (ERD)

<div class="cols">
<div class="small">

`users (1) ──< transcripts (1) ──< segments`

| 테이블 | 핵심 컬럼 |
| --- | --- |
| **users** | id(PK), username(UQ), password_hash |
| **transcripts** | id(PK), **user_id(FK)**, source, raw_text, summary, keywords, memo, audio_path, created_at |
| **segments** | id(PK), **transcript_id(FK)**, start_sec, end_sec, speaker, content |

</div>
<div>

### 핵심 설계 결정
- **1:N 관계** — 회원 1명 ↔ 기록 N개 ↔ 구간 N개
- **UUID 기본 키** — 분산·병합에 안전한 고유 ID
- **ON DELETE CASCADE** — 회원 탈퇴 시 하위 데이터 자동 정리
- **트랜잭션 저장** — 본문+구간을 원자적으로 (실패 시 롤백)
- **`?` 파라미터 바인딩** — SQL 인젝션 방지

</div>
</div>

<!--
[60초] DB는 교수님 관심사. 설계 '결정과 이유'를 강조.
"세 테이블을 외래 키로 연결하고, 무결성을 위해 CASCADE와 트랜잭션을 적용했습니다."
[예상 질문] "트랜잭션을 왜 쓰나?" → "한 기록은 transcripts 한 줄과 segments 여러 줄로 이뤄지는데, 중간에 실패하면 반쪽 데이터가 남습니다. 트랜잭션으로 전부 성공 또는 전부 취소를 보장합니다."
[예상 질문] "CASCADE를 코드로 어떻게 켰나?" → "SQLite는 외래키가 기본 비활성이라 연결마다 PRAGMA foreign_keys=ON을 실행합니다."
[예상 질문] "왜 정수 대신 UUID?" → "기록 생성 시점에 클라이언트에서 충돌 없이 ID를 만들 수 있고, 나중에 외부 동기화로 확장해도 안전합니다."
-->

---

<!-- _class: lead -->

# 3. 어떻게

## 구현

---

## 프로젝트 구조 & 요구사항 추적성

<div class="cols">
<div class="small">

```
src/main/java/
├── SpeechNoteApp.java  # 진입점
├── audio/    녹음·장치
├── api/      STT·LLM 통신
├── service/  변환·인증·내보내기
├── db/       DB 접근(DAO)
├── ui/       Swing 화면
└── common/   모델·설정·예외·로깅
```

</div>
<div class="small">

| 패키지 | 요구사항 | 핵심 기술 |
| --- | --- | --- |
| `audio` | F-01,02 | Java Sound, 채널 분리 |
| `api` | F-03,13,14 | HttpClient, 비동기·재시도 |
| `service` | F-05,06,10,11,17 | 파이프라인·인증·POI |
| `db` | F-07,12,15,16 | JDBC, 트랜잭션, CASCADE |
| `ui` | F-08,09,18,20 | Swing, SwingWorker |
| `common` | F-04,19 | 설정·모델·로깅 |

</div>
</div>

<div class="why">
<b>추적성</b>: 모든 요구사항(F-01~20)이 특정 패키지·소스 파일과 1:1로 연결됨 → "요구사항이 실제로 구현됐는가"를 코드로 증명
</div>

<!--
[40초] 추적성(traceability)은 문서화 과목에서 점수 포인트.
"요구사항 → 패키지 → 소스 파일이 추적되도록 설계했습니다. 어떤 요구사항이든 어느 파일에서 구현됐는지 바로 짚을 수 있습니다."
[예상 질문] "F-13 요약은 어디 있나?" 같은 질문에 → "api/LlmClient.java와 service/TranscriptionService.java입니다." 즉답할 수 있게 매핑 외워두기.
-->

---

## 핵심 구현 ① 입력 → 변환 파이프라인

<div class="cols">
<div>

**🎙 녹음 (F-01)**
- 마이크 + 시스템 두 라인을 **동시에** 읽어 2채널로 저장 → 16kHz WAV
- 실시간 입력 레벨(RMS) 미터, 일시정지·취소

**📤 STT 통신 (F-03)**
- multipart로 음성 파일 전송
- **재시도 + 타임아웃** 적용

</div>
<div>

**⚙️ 비동기 처리 (F-05)**
- `CompletableFuture` / `SwingWorker`로 백그라운드 변환
- 진행바·상태 메시지, **취소 가능**

**🧩 결과 모델**
- 응답을 `TranscriptResult`(원문 + 시간대별 `segment[]`)로 파싱
- 화면·DB·내보내기에서 공통 사용

</div>
</div>

<!--
[50초] 기술적 깊이를 보여주는 첫 장.
"녹음은 두 오디오 라인을 동시에 읽어 한 파일로 합치고, 전송 효율을 위해 16kHz로 다운샘플합니다. 변환은 시간이 걸려 비동기로 처리하고, 사용자가 중간에 취소할 수 있게 했습니다."
[예상 질문] "변환 중 창이 멈추지 않나?" → "STT 호출을 별도 스레드(CompletableFuture)에서 실행하고 결과만 UI 스레드로 돌려받아, 변환 중에도 화면이 멈추지 않고 취소도 가능합니다."
-->

---

## 핵심 구현 ② 관리 · AI · 내보내기

<div class="cols">
<div>

**🗂 기록 관리**
- 사용자별 기록 분리, 좌측 히스토리 패널
- **실시간 통합 검색**(메모·원문·요약·키워드) + 정렬
- 우클릭 메뉴(복사·내보내기·삭제)
- **메모 자동 저장** (입력 멈춤 시 저장·이탈 시 flush)

</div>
<div>

**✨ AI 어시스턴트 (F-13·14)**
- LLM으로 **3줄 요약 + 키워드 5개** 추출

**📦 내보내기 (F-10·11)**
- **TXT** (원문+메모+요약)
- **SRT** 자막 (타임코드·화자)
- **DOCX** 문서 (Apache POI)

</div>
</div>

<!--
[50초] 사용자가 체감하는 가치 기능들.
"변환에서 끝나지 않고, 검색·메모·요약·자막/문서 내보내기까지 실제 활용 흐름을 완성했습니다. 특히 메모는 버튼을 누르지 않아도 입력이 멈추면 자동 저장돼 데이터 유실을 막습니다."
[예상 질문] "검색이 느리지 않나?" → "로컬 메모리 리스트를 즉시 필터링하는 방식이라 기록 수백 건 규모에선 즉각 반응합니다."
-->

---

## 기술적 난제와 해결

<div class="cols3 small">
<div class="box">

**① 시스템(스피커) 음성 캡처**
OS가 기본 제공하지 않음
→ **VB-CABLE 가상 장치**로 루프백, 마이크와 **2채널 동시 녹음** 후 합성

</div>
<div class="box">

**② 깨지거나 잘린 STT 응답**
JSON이 중간에 끊겨 옴
→ **정규식 fallback 파싱**으로 세그먼트 복구(`SttResponse`) → 죽지 않고 결과 표시

</div>
<div class="box">

**③ 긴 작업 중 UI 멈춤·메모 유실**
→ **비동기 + 취소** 처리, 메모는 **디바운스 자동 저장 + 이탈 시 flush**로 안전 보장

</div>
</div>

<div class="why">
"되게 만들기"를 넘어 <b>안 될 때를 대비</b>한 설계 — 네트워크 오류·깨진 응답·중도 취소에도 앱이 죽지 않음
</div>

<!--
[60초] 발표의 하이라이트이자 최강 방어 슬라이드. 실제로 어려웠던 걸 어떻게 풀었는지가 실력을 증명.
3개 중 ①(시스템 음성)을 가장 강조. "윈도우는 스피커로 나가는 소리를 그냥은 못 녹음합니다. VB-CABLE 가상 오디오 장치로 출력을 되돌려 입력으로 받고, 마이크와 합쳐 2채널로 저장했습니다."
[예상 질문] "VB-CABLE 없으면 안 되나?" → "시스템 음성 캡처에는 가상 장치가 필요합니다. 마이크 단독 녹음·파일 변환은 추가 설치 없이 동작합니다. README에 설치 안내를 넣었습니다."
[예상 질문] "fallback 파싱이 정확한가?" → "정상 JSON이면 정식 파싱하고, 깨진 경우에만 정규식으로 텍스트·타임코드를 최대한 복구합니다. 완벽 복구는 아니지만 앱이 죽는 대신 부분 결과라도 보여줍니다."
-->

---

## 데모 — 실제 동작 화면

<div class="cols">
<div>

**시연 시나리오**
1. 로그인
2. 오디오 파일 업로드 → **변환**
3. 좌측 기록 목록 · 검색
4. **AI 요약/키워드** 생성
5. 메모 작성(자동 저장)
6. **DOCX/SRT 내보내기**

</div>
<div>

<!-- 스크린샷을 docs/images/ 에 넣고 아래 경로로 교체하세요 -->
<!-- ![w:520](images/demo-main.png) -->

> 📷 **여기에 메인 화면 스크린샷**
> (Two-Split: 음성 기록 ↔ 메모, 우측 AI 패널)
>
> 라이브 데모가 가능하면 실제 앱으로 1·2·4번을 직접 시연

</div>
</div>

<!--
[데모 2~3분] 발표에서 가장 임팩트 큰 부분. 가능하면 라이브 시연, 불안하면 미리 캡처한 스크린샷/짧은 녹화로 대체.
시연 전 체크: API 키 입력 완료, 샘플 오디오 준비, 인터넷 연결, (시스템 음성 데모 시) VB-CABLE 선택.
라이브가 위험하면: "네트워크 상황을 감안해 미리 녹화한 화면으로 보여드리겠습니다." 라고 안전하게.
[예상 질문] "지금 바로 녹음해서 보여줄 수 있나?" → 준비됐으면 시연, 아니면 "녹음→변환은 파일 변환과 동일 경로이며 스크린샷으로 결과를 준비했습니다."
※ 발표 전 docs/images/ 에 스크린샷을 넣고 이 슬라이드의 주석(<!-- ![...] -->)을 풀어 실제 이미지로 교체할 것.
-->

---

## 테스트 & 최적화

<div class="cols">
<div>

### ✅ 검증 방법
- `Demo.java` 콘솔로 **STT 연동 단독 검증**
- 기능별 **수동 시나리오** 점검
  (녹음→변환→저장→내보내기)
- **오류 상황 테스트**: 네트워크 끊김·잘못된 입력·중도 취소 → 종료 없이 메시지

</div>
<div>

### ⚡ 최적화 · 안정성
- segments **배치 INSERT**로 저장 성능↑
- 오디오 **16kHz 다운샘플**로 전송량↓
- API **재시도 + 타임아웃**
- **전역 에러 로깅** (`logs/error.log`)
- 사용자별 기록 분리 검증

</div>
</div>

<!--
[40초] "테스트는 했나"에 대한 답.
"자동화 단위테스트까지는 범위상 못 했지만, STT 연동을 Demo로 분리 검증했고, 핵심 흐름을 시나리오 기반으로 점검했으며, 특히 실패 상황(오류·취소)에서 앱이 죽지 않는지를 중점 확인했습니다."
[예상 질문] "JUnit 단위테스트는?" → 솔직하게: "이번엔 수동·시나리오 검증 위주였고, JUnit 의존성은 추가해 두었습니다. 자동화 테스트 확대는 개선 과제로 남겨두었습니다." (거짓말 금지 — 정직이 최선의 방어)
-->

---

## 빌드 · 배포 · 산출물

<div class="cols">
<div>

### 🛠 빌드 & 실행
```bash
mvn clean package
java -jar \
  target/SpeechNote-1.0-SNAPSHOT.jar
```
- **maven-shade-plugin**으로 의존성 포함 **단일 JAR**
- **GitHub Releases**에 업로드 → 설치 없이 실행

</div>
<div>

### 📄 프로젝트 산출물
- **README** — 소개·기술스택·실행법·구조·VB-CABLE 안내
- **.gitignore** — `target/`, `config.properties`(API 키), `*.db`, `logs/` 제외
- **docs/** — 0~4·9 설계 문서 + 발표 슬라이드
- **GitHub Issues** — 요구사항 추적

</div>
</div>

<div class="why">
API 키·로컬 DB 등 <b>민감/생성 파일은 커밋 제외</b> → 보안 + 저장소 청결
</div>

<!--
[40초] Release(07)·README/gitignore(09) 요구사항을 한 번에 커버.
"단일 JAR로 패키징해 JRE만 있으면 바로 실행되도록 배포했습니다. API 키와 로컬 DB는 .gitignore로 커밋에서 제외했습니다."
[예상 질문] "API 키가 깃에 올라가 있진 않나?" → ".gitignore에 config.properties를 넣어 키는 저장소에 포함되지 않습니다." (실제 확인해 둘 것!)
-->

---

## 협업 — 역할 · Git · 일정

<div class="cols">
<div class="small">

### 👥 역할 분담
| 담당 | 영역 (요구사항) |
| --- | --- |
| **민건영** | 입력·오디오·API (F-01~05,13,14) |
| **정의영**(팀장) | UI·DB·관리 (F-06~12,15~20) |

경계 = **공통 객체 `TranscriptResult`** → 충돌 최소화

### 🔀 Git 프로세스
**브랜치 → PR → 리뷰 → merge** + Issues(라벨·담당·마일스톤)

</div>
<div class="small">

### 📅 일정 (WBS, 3주)
| 주차 | 산출물 |
| --- | --- |
| 1주 (5/25~31) | MVP: 파일→텍스트, DB 스키마 |
| 2주 (6/1~7) | 녹음→변환→저장, 히스토리·TXT |
| 3주 (6/8~14) | 시스템음성·AI·검색·DOCX/SRT·로그인·배포 |

중간 점검 ①STT변환 ②녹음·저장 ③최종 통합·배포

</div>
</div>

<!--
[50초] "협업했나 / 한 사람이 다 한 거 아니냐 / 공평하냐" 방어.
"입력 라인은 민건영, 출력·관리 라인은 정의영이 맡고, 공통 도메인 객체로 경계를 정해 병렬 작업했습니다. 의견은 Teams·Notion에서 모으고, 코드는 브랜치-PR-리뷰-머지로 반영했습니다."
[예상 질문] "커밋이 한 사람한테 쏠려 있지 않나?" → 솔직하게 분담 근거(요구사항 매핑)와 협업 도구를 설명. 필요하면 Insights/Contributors 그래프 준비.
[예상 질문] "일정대로 됐나?" → "1·2주차 MVP는 일정대로, 3주차에 시스템 음성·AI를 마무리했습니다."
-->

---

<!-- _class: lead -->

# 4. 마무리

## 현황 · 회고 · 결론

---

## 구현 현황 요약

<div class="cols">
<div>

<p class="big">20 / 20</p>

**요구사항 100% 구현 완료**

- ✅ 완성 20 · 🔶 부분 0 · ❌ 미완 0

</div>
<div>

### 완결성의 의미
- 핵심(녹음·변환·저장)부터
  부가(검색·내보내기·계정)까지
  **하나의 흐름으로 연결**
- 단순 기능 나열이 아니라
  **실사용 가능한 완성도**

</div>
</div>

<!--
[20초] 숫자로 임팩트. "정의한 20개 요구사항을 모두 구현했고, 각 기능이 따로 노는 게 아니라 녹음부터 내보내기까지 하나의 사용 흐름으로 이어집니다."
[예상 질문] "100%라는데 정말 다 되나?" → 데모/스크린샷으로 이미 증명했음을 환기.
-->

---

## 회고 — 잘된 점과 한계

<div class="cols">
<div>

### 👍 잘된 점
- **계층 분리** 설계 → 유지보수·분담 용이
- **견고함**: 재시도·타임아웃·트랜잭션·fallback 파싱
- 오류에도 **죽지 않는** 안정성
- 요구사항 **추적성** 확보

</div>
<div>

### 🔧 한계 & 개선 방향
- **① 실시간 전사 미구현**
  일괄 처리 → *WebSocket 스트리밍 STT*로 발화와 동시 자막
- **② 로컬 DB 한계**
  단일 PC 종속 → *중앙 DB/백엔드*로 다중 기기·백업·공유

</div>
</div>

<!--
[50초] 한계를 스스로 인정 = 가장 강력한 딴지 방어. "못한 것"이 아니라 "인지하고 개선안을 가진 것"으로 프레이밍.
"두 가지 한계를 명확히 인지하고 있고, 각각 구체적 개선 방향을 가지고 있습니다."
[예상 질문] "이걸 왜 지금 안 했나?" → "3주 일정·인프라 제약 안에서 완결된 앱을 우선했고, 두 개선안은 아키텍처상 확장 지점(STT 통신 모듈·DAO 계층)이 이미 분리돼 있어 교체가 용이합니다."
이 답이 계층 분리 설계의 가치를 다시 부각시킴.
-->

---

<!-- _class: lead -->
<!-- _paginate: false -->

# 감사합니다 🙏

## SpeechNote — 마이크와 시스템 음성을 함께 기록하는 AI 메모장

**정의영 · 민건영**
github.com/jeyman2003-debug/SpeechNote

### 질문 환영합니다

<!--
[클로징] "이상으로 발표를 마치겠습니다. 마이크와 시스템 음성을 함께 기록하고 AI 요약까지 제공하는 SpeechNote였습니다. 질문 주시면 답변드리겠습니다. 감사합니다."
미리 준비할 추가 답변 거리: ① 화자 분리(speaker) 동작 방식 ② config.properties 구조 ③ FlatLaf 적용 이유 ④ 향후 확장 계획.
-->
